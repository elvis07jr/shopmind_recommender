# ShopMind: E-Commerce Recommendation Engine
# Based on Apriori, Streamlit, and UCI Dataset

pandas>=1.5.0
numpy>=1.24.0
matplotlib>=3.6.0
seaborn>=0.12.0
scikit-learn>=1.3.0
mlxtend>=0.28.0
streamlit>=1.24.0
jupyter>=1.0.0
ucimlrepo>=0.0.5

# Optional: For better table display and data handling
notebook
tqdm

# Lock exact versions (optional — use if you want full reproducibility)
# To generate exact versions, run:
#   pip freeze > requirements.txt